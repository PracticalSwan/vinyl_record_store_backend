MONGODB_URI=
MONGODB_DB_NAME=vinyl_record_store
CATALOG_DATA_SOURCE=seed
FRONTEND_ORIGIN=http://localhost:5173
# Netlify production sets this to the exact Groovehaus storefront origin.
ARTWORK_CACHE_DIR=
# Netlify production uses a writable temporary directory such as /tmp/groovehaus-artwork-images.
RECOMMENDER_ALGORITHM_VERSION=content-demo-v1
PERS_IDENTITY_STRICT=true
PERS_ME_ENDPOINT=true
PERS_PROFILE_DOMAIN=false
PERS_PREFERENCE_RANKING=false
PERS_NEGATIVE_FEEDBACK=false
PERS_BEHAVIORAL_RANKING=false
PERS_POPULARITY=false
PERS_HYBRID=false
DATASET_PSEUDONYM_KEY=
# A dedicated 32+ character secret is preferred. When absent, the staging CLI
# uses AUTH_SECRET with domain separation. Never commit either value.
MUSICBRAINZ_USER_AGENT=GroovehausAcademic/0.1 (https://github.com/PracticalSwan/vinyl_record_store_backend)
AUTH_SECRET=
# Set true for HTTPS production deployments.
AUTH_COOKIE_SECURE=false
# Showcase customer accounts are seeded into MongoDB by `npm run db:seed:users:apply`
# (see src/data/demoUsers.js). The administrator account is environment-only.
# Never commit real hashes, salts, or AUTH_SECRET.
# In Next.js .env.local files, escape each $ in a scrypt hash as \$; Netlify env vars are literal.
AUTH_DEMO_ADMIN_USERNAME=
AUTH_DEMO_ADMIN_DISPLAY_NAME=
AUTH_DEMO_ADMIN_PASSWORD_HASH=
AUTH_DEMO_ADMIN_PASSWORD_SALT=
